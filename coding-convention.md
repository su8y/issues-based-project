# 코딩 컨벤션 가이드

기본적으로 코딩에 대한 컨벤션은 PR 단위로 CI(Continous Integration)를 통하여 검사를 한다.
하지만, 코딩에 대한 컨벤션은 자율로 정한다.

자율로 정하는 이유
- 프로젝트와 언어 프레임워크 별로 권장하는 코딩 규칙이 다름.
- 팀 별로 이미 정해진 코딩 규칙이 있기 때문에 추가 학습 곡선을 추가하지 않으려는 목적